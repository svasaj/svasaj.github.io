# Svajūnas Sajavičius --- 24 Academic Pages publication files

Copy these files into `_publications/`.

Categories used: `preprints`, `manuscripts`, `books`, `conferences`,
`reports`, `abstracts`.

If your Academic Pages version does not define these categories, add
them under `publication_category` in `_config.yml`.

The 2012 *Computers & Mathematics with Applications* article uses the
verified page range **3485--3499**.
